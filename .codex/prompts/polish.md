---
description: "Polish a WolfBBS UI surface using the local Impeccable design skills"
argument-hint: "[route or surface]"
---

Use the local Impeccable skills in `.codex/skills`.

Steps:
1. Read `.impeccable.md` for WolfBBS design context.
2. Use the `frontend-design` skill plus the `polish` skill guidance.
3. If no argument is provided, polish the shared web shell plus the densest routes: `/start`, `/boards`, `/chat`, `/admin/setup`, and `/admin/config`.
4. Make real code changes, not just recommendations.
5. Re-run the relevant UI validation after changes and summarize what improved.

Target: $ARGUMENTS

{
  "name": "wolfbbs-web-e2e",
  "version": "0.1.0",
  "private": true,
  "description": "Playwright end-to-end tests for WolfBBS web user/admin flows",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "install:browsers": "playwright install chromium",
    "screenshots": "WOLFBBS_CAPTURE_SCREENSHOTS=1 playwright test tests/docs_screenshots.spec.js"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.11.0",
    "@playwright/test": "^1.52.0"
  }
}

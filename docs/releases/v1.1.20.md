# WolfBBS v1.1.20

## Highlights
- Closed the remaining SSH/TUI parity gaps so both caller and sysop workflows now have matching terminal surfaces instead of web-only dead ends.
- Added shared runtime error persistence so web admin and SSH ops see the same failure history and can clear it from either side.
- Refreshed the public release/docs surface to point at the current distro target, packaging commands, and release index.

## Shipped Features
- Full caller parity for collections, offline packets, handle suggestions, password reset flows, bookmarks, circles, profile export, attention export, showcase, and status snapshots.
- Full admin parity for config/setup, gateways, chat ops, release tooling, doors, events, bulletins, files, mail, challenges, login/auth, and ops/errors.
- Shared runtime error store backing `/admin/errors` and the SSH ops center.
- Updated release metadata across the README, docs hub, install guide, datasheet, and showcase pages for the `v1.1.20` distro bundle.

## Validation
- `go test ./...`
- `scripts/verify.sh --fast`
- `scripts/verify.sh --smoke`
- `scripts/manual-acceptance.sh --auto --no-smoke --report docs/manual-acceptance-latest.md`

## Acceptance
- Manual acceptance report: `PASS=21 FAIL=0 SKIPPED=0`

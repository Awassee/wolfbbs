# WolfBBS v1.1.21

## Highlights
- Promoted the fully green post-release line to a new tag so the public release matches the commit with passing GitHub installer and smoke workflows.
- Cleaned up installer shellcheck warnings without changing installer behavior.
- Refreshed public release/docs pointers so packaging, README, install guide, datasheet, and showcase all point at the current distro tag.

## Shipped Changes
- Installer cleanup in `install.sh` for compose-less cleanup and diagnostics bundle output paths.
- Release metadata refresh across the README, docs hub, install guide, datasheet, showcase page, and release index.
- Fresh `v1.1.21` distro bundles and checksums.

## Validation
- `shellcheck install.sh bootstrap.sh scripts/verify.sh scripts/test-installer-regressions.sh`
- `scripts/verify.sh --fast`
- GitHub Actions on the release line:
  - `installer-check`
  - `integration-smoke`

## Acceptance
- The application/runtime validation remains green from the `v1.1.20` ship candidate:
  - `go test ./...`
  - `scripts/verify.sh --smoke`
  - `scripts/manual-acceptance.sh --auto --no-smoke --report docs/manual-acceptance-latest.md`
- Manual acceptance summary: `PASS=21 FAIL=0 SKIPPED=0`

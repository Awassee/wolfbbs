# v2.1.5

- fix the macOS installer path so Homebrew/Colima installs wire the Docker Compose and Buildx CLI plugins correctly
- prefer the packaged release bundle over an older managed git checkout during app sync, avoiding accidental source builds in consumer installs
- improve installer dependency/setup guidance, port conflict handling, and status output polish

Validation:

- `bash scripts/test-installer-regressions.sh`
- `scripts/verify.sh --fast`

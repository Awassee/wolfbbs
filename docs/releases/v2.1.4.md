# v2.1.4

- Simplified the macOS Docker bootstrap flow so first-run installs use one guided choice instead of chained yes/no prompts.
- Made installer confirmations more forgiving by accepting full `yes` responses and supporting recommended defaults cleanly.
- Cleaned up macOS-built release tarballs so Linux extraction no longer emits Apple extended-attribute warnings.
- Verified the bundle install lifecycle on a fresh DigitalOcean Ubuntu 24.04 droplet: install, health checks, `--status`, and `--clean-uninstall --purge`.

Validation highlights:

- `bash scripts/test-installer-regressions.sh`
- `scripts/verify.sh --fast`
- local package build for `v2.1.4-rc2` on `linux/amd64`
- real external install and uninstall test on DigitalOcean Ubuntu 24.04

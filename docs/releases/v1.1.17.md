# WolfBBS v1.1.17

## Highlights
- Shipped the next ordered product tranche from `docs/ROADMAP_101_150.md` items `121-130`.
- Added richer caller identity, relationship, and coordination workflows across directory, settings, events, and admin mail.
- Closed concrete workflow defects in poll voting, local mail targeting, and admin mail flash handling.

## Shipped Features
- Shared moderator inbox with assignment and resolve state in `/admin/mail`.
- Sysop-targeted internal mail merge with handle personalization in `/admin/mail`.
- Caller profile status line, bio card, and verified contact preferences from `/settings`.
- Per-contact aliases and caller circles with a dedicated `/circles` route.
- Relationship timeline and moderator-visible incident timeline on caller profiles in `/directory`.
- Presence-aware RSVP and invitation flow tied to `/events` and caller profiles.
- Profile export and privacy-state JSON via `/profile/export`.
- New roadmap extension in `docs/ROADMAP_201_250.md`.

## Bug Fixes
- Frozen poll threads now reject votes instead of accepting state changes after moderation freeze.
- Local private mail compose now rejects disabled and banned local recipients.
- `/admin/mail` now renders flash notices so success and error redirects are actually visible.

## Validation
- `go test ./...`
- `scripts/verify.sh --fast`
- `scripts/run-e2e.sh --no-go --no-tui --web-timeout 900`
- `WOLFBBS_SSH_PORT=42222 WOLFBBS_WEB_PORT=48080 WOLFBBS_IRC_PORT=46667 WOLFBBS_IRC_TLS_PORT=46697 scripts/verify.sh --smoke`
- `scripts/manual-acceptance.sh --auto --no-smoke --report docs/manual-acceptance-latest.md`

## Acceptance
- Manual acceptance report: `PASS=21 FAIL=0 SKIPPED=0`

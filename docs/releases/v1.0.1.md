# WolfBBS v1.0.1

Patch release focused on release hygiene.

## Highlights

- Removed machine-specific checkout paths from manual acceptance docs.
- Removed the tracked `install.log` artifact from the repository.
- Ignored `install.log` going forward so local installer runs stay out of source control.
- Rebuilt distribution bundles from the sanitized tree.

## Validation

- Tracked-file grep audit for local path leakage: clean
- Distribution tarball grep audit for local path leakage: clean
- Representative binary `strings` audit for local build paths: clean

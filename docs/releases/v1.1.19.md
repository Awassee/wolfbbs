# WolfBBS v1.1.19

## Highlights
- Shipped roadmap tranche `141-145` from `docs/ROADMAP_101_150.md`.
- Added a full community retention loop: event attendance, post-event recaps, seasonal challenges, and shared clubhouse goals.
- Added operator safety surfaces for upgrades and backup artifact validation in both web admin and installer status output.

## Shipped Features
- Event attendance tracking and recap publishing in `/events`, `/admin/events`, and `/events/recaps`.
- Seasonal challenge scoring across boards, chat, and doors in `/challenges` with admin controls in `/admin/challenges`.
- Clubhouse shared goals tied to boards/doors with caller contribution flow in `/clubhouse`.
- Upgrade safety dashboard in `/admin/upgrade-safety` plus installer status safety section in `install.sh --status`.
- Backup browser and artifact validation in `/admin/backups`.

## Validation
- `go test ./...`
- `scripts/verify.sh --fast`
- `scripts/verify.sh --smoke`
- `scripts/manual-acceptance.sh --auto --no-smoke --report docs/manual-acceptance-latest.md`

## Acceptance
- Manual acceptance report: `PASS=21 FAIL=0 SKIPPED=0`

# WolfBBS v1.1.18

## Highlights
- Shipped the next ordered product tranche from `docs/ROADMAP_101_150.md` items `131-140`.
- Expanded the FileBase into a fuller caller workflow with requests, curated collections, offline packets, and richer admin moderation tooling.
- Added targeted regression coverage for the new file and offline surfaces, then validated the full release stack.

## Shipped Features
- Caller file request queue with ownership and fulfillment workflow in `/gateway?view=files` and `/admin/files`.
- Collaborative upload drafts, curator notes, featured collections, and repair actions in `/admin/files`.
- Duplicate detection, uploader reputation tiers, related door packs, and related event bundles on file preview pages.
- New curated collections route at `/collections`.
- New offline packet and offline mail import route at `/offline`.

## Validation
- `go test ./...`
- `scripts/verify.sh --fast`
- `scripts/run-e2e.sh --no-go --no-tui --web-timeout 900`
- `WOLFBBS_SSH_PORT=42222 WOLFBBS_WEB_PORT=48080 WOLFBBS_IRC_PORT=46667 WOLFBBS_IRC_TLS_PORT=46697 scripts/verify.sh --smoke`
- `scripts/manual-acceptance.sh --auto --no-smoke --report docs/manual-acceptance-latest.md`

## Acceptance
- Manual acceptance report: `PASS=21 FAIL=0 SKIPPED=0`

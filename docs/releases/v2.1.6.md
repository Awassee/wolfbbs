# v2.1.6

- fail fast when a legacy `docker-compose` backend would try to build the source checkout without modern `docker compose` and Buildx support
- tighten packaged bundle detection so incomplete app directories do not masquerade as release bundles
- expand installer regression coverage for bundle integrity and compose backend safety checks

Validation:

- `bash scripts/test-installer-regressions.sh`
- `scripts/verify.sh --fast`
